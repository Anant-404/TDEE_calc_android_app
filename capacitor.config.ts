import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anantzerozerotwo.caloriecalc',
  appName: 'caloriecalc',
  webDir: 'public'
};

export default config;
